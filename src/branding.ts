/** Müvekkil Kasası — marka görselleri (Vite URL). */
import programIconUrl from './assets/branding/app-icon.png'
import programLogoUrl from './assets/branding/logo.png'
import woontegraMarkUrl from './assets/branding/woontegra-logo.png'

/** Favicon ile aynı ikon (sidebar, küçük marka alanları). */
export const PROGRAM_ICON_SRC: string = programIconUrl
/** Tam logo (metinli) — giriş hero vb. geniş alanlar. */
export const PROGRAM_LOGO_SRC: string = programLogoUrl
export const WOONTEGRA_MARK_SRC: string = woontegraMarkUrl
